import { StyleSheet, Text, View } from 'react-native';

export default function FreightScreen() {
	return (
		<View>
			<Text>FreightScreen</Text>
		</View>
	);
}

const styles = StyleSheet.create({});
